# Fallback only. The desktop starts swaybg (scripts/wallpaper.sh) because it
# also renders on virtual GPUs. To use hyprpaper instead, swap the exec-once in
# hyprland.conf and uncomment this block.
# wallpaper {
#     monitor =
#     path = ~/.local/share/wallpapers/warm-night.png
#     fit_mode = cover
# }
splash = false
ipc = off

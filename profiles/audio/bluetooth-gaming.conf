# Optional low-dropout Bluetooth gaming profile.
# Copy this over 50-bluetooth-balanced.conf when headset microphone mode is not
# needed and A2DP stability is more important than codec choice.
monitor.bluez.properties = {
    bluez5.roles = [ a2dp_sink ]
    bluez5.codecs = [ sbc ]
}
